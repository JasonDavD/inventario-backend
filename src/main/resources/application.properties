# ===== Configuracion por defecto (desarrollo con H2) =====
spring.application.name=inventario

# H2 Database (desarrollo local)
spring.datasource.url=jdbc:h2:mem:inventariodb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ===== Configuracion de produccion (Render - PostgreSQL) =====
# Estas propiedades se activan via variables de entorno en Render:
# spring.datasource.url=${DATABASE_URL:}
# spring.datasource.username=${DATABASE_USER:}
# spring.datasource.password=${DATABASE_PASSWORD:}
